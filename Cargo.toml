[package]
name = "WhatSpace"
version = "0.1.0"
edition = "2021"

[dependencies]
uuid = { version = "1.22.0", features = ["v4", "serde"] }   # to gnenerate the unique id
chrono = { version = "0.4", features = ["serde"] } # timestamp gestio

[build-dependencies]
protobuf-codegen = "3"